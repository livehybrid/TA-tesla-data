[tool.poetry]
name = "TA-tesla-data"
version = "0.1.0"
description = ""
authors = ["Will Searle <will@livehybrid.com>"]

[tool.poetry.dependencies]
python = "^3.10"
splunk-add-on-ucc-framework = "^5.14.1"
requests = "^2.28.1"
htmldom = "^2.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
